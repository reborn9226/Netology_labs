---
- name: Настройка веб сервера nginx
  hosts: node
  become: yes

  tasks:
    - name: Установить пакет nginx
      ansible.builtin.apt:
        name: nginx
        state: present

    - name: Убедится что nginx запущен
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes